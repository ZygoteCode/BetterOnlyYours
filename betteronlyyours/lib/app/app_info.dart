/// Static product facts, kept in one place so copy stays consistent.
class AppInfo {
  const AppInfo._();

  static const String name = 'BetterOnlyYours';
  static const String version = '2.0.0';
  static const String tagline = 'A local, encrypted vault that stays yours.';
  static const String description =
      'BetterOnlyYours keeps credentials and secrets in a single encrypted '
      'file on this machine. No account, no sync, no telemetry — the vault is '
      'only readable with your master password.';
}
